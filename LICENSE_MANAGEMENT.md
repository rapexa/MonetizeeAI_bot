# راهنمای مدیریت لایسنس‌ها

## 📋 خلاصه

سیستم مدیریت لایسنس‌های از پیش تولید شده به شما امکان می‌دهد لایسنس‌های معتبر تولید کنید که کاربران می‌توانند مستقیماً در ربات استفاده کنند و بدون نیاز به تایید ادمین، اشتراک مادام‌العمر دریافت کنند.

## 🔑 ویژگی‌ها

- ✅ هر لایسنس فقط **یک‌بار** قابل استفاده است
- ✅ فعال‌سازی **خودکار** بدون نیاز به تایید ادمین
- ✅ فرمت استاندارد: `XXXX-XXXXX-XXXXX-XXXXX`
- ✅ مدیریت کامل از پنل ادمین
- ✅ آمار لحظه‌ای در داشبورد

## 🚀 تولید لایسنس‌ها

### روش 1: از طریق پنل ادمین (توصیه می‌شود)

1. وارد پنل ادمین شوید
2. به تب **"لایسنس‌ها"** بروید
3. روی دکمه **"تولید لایسنس"** کلیک کنید
4. تعداد مورد نظر را وارد کنید (1 تا 1000)
5. لایسنس‌ها به صورت خودکار تولید و ذخیره می‌شوند

### روش 2: از طریق اسکریپت Go

```bash
# تنظیم متغیر محیطی (اختیاری)
export DATABASE_URL="user:password@tcp(localhost:3306)/database_name?charset=utf8mb4&parseTime=True&loc=Local"

# اجرای اسکریپت (پیش‌فرض: 500 لایسنس)
go run cmd/generate-licenses/main.go

# یا با تعداد دلخواه
go run cmd/generate-licenses/main.go 1000
```

این اسکریپت به صورت پیش‌فرض **500 لایسنس** تولید می‌کند، اما می‌توانید تعداد دلخواه را به عنوان آرگومان وارد کنید.

## 📊 مشاهده آمار

### در داشبورد اصلی:
- کارت **"لایسنس‌های از پیش تولید شده"** در داشبورد نمایش داده می‌شود
- شامل: کل لایسنس‌ها، استفاده شده، استفاده نشده

### در تب لایسنس‌ها:
- آمار کامل در بالای صفحه
- لیست تمام لایسنس‌ها با جزئیات
- فیلتر بر اساس وضعیت (همه، استفاده شده، استفاده نشده)
- جستجو در لایسنس‌ها

## 👤 استفاده کاربران

کاربران می‌توانند لایسنس را در ربات وارد کنند:

1. کاربر لایسنس را در ربات وارد می‌کند
2. سیستم بررسی می‌کند که لایسنس معتبر و استفاده‌نشده است
3. اگر معتبر باشد:
   - لایسنس به عنوان استفاده‌شده علامت‌گذاری می‌شود
   - اشتراک مادام‌العمر (`ultimate`) برای کاربر فعال می‌شود
   - کاربر می‌تواند بلافاصله از تمام امکانات استفاده کند

## 🔒 امنیت

- هر لایسنس فقط یک‌بار قابل استفاده است
- لایسنس‌های استفاده‌شده دیگر قابل استفاده نیستند
- اطلاعات کاربری که لایسنس را استفاده کرده در پنل ادمین ثبت می‌شود

## 📝 نکات مهم

1. **تولید انبوه**: می‌توانید تا 1000 لایسنس در یک بار تولید کنید
2. **فرمت لایسنس**: همه لایسنس‌ها به فرمت `XXXX-XXXXX-XXXXX-XXXXX` هستند
3. **عدم نیاز به تایید**: برخلاف سیستم قدیمی، لایسنس‌های جدید نیاز به تایید ادمین ندارند
4. **سازگاری**: سیستم قدیمی لایسنس‌ها همچنان کار می‌کند

## 🛠️ API Endpoints

### دریافت آمار
```
GET /api/v1/admin/license-keys/stats
```

### دریافت لیست لایسنس‌ها
```
GET /api/v1/admin/license-keys?page=1&limit=50&status=all&search=
```

### تولید لایسنس‌های جدید
```
POST /api/v1/admin/license-keys/generate
Body: { "count": 500 }
```

### دریافت جزئیات یک لایسنس
```
GET /api/v1/admin/license-keys/:id
```

## 📈 آمار در داشبورد

آمار لایسنس‌ها در بخش‌های زیر نمایش داده می‌شود:

1. **داشبورد اصلی**: کارت "لایسنس‌های از پیش تولید شده"
2. **تب لایسنس‌ها**: آمار کامل و لیست لایسنس‌ها
3. **Stats API**: فیلدهای `totalLicenseKeys`, `usedLicenseKeys`, `unusedLicenseKeys`

## 🔄 تفاوت با سیستم قدیمی

| ویژگی | سیستم قدیمی | سیستم جدید |
|--------|-------------|------------|
| نیاز به تایید | ✅ بله | ❌ خیر |
| فعال‌سازی | دستی توسط ادمین | خودکار |
| تعداد | نامحدود | محدود (از پیش تولید شده) |
| استفاده مجدد | ❌ | ❌ (هر دو یک‌بار) |

## 💡 پیشنهادات

1. لایسنس‌ها را به صورت دسته‌ای تولید کنید (مثلاً 500 تایی)
2. لایسنس‌های استفاده نشده را برای مشتریان جدید نگه دارید
3. از جستجو و فیلتر برای مدیریت بهتر استفاده کنید
4. آمار را به صورت منظم بررسی کنید

