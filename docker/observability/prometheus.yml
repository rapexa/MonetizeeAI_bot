global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "monetizee-api"
    static_configs:
      - targets: ["host.docker.internal:8080"]
    metrics_path: /metrics
    # host.docker.internal works on Mac/Windows; for Linux use host IP or network_mode: host
